[2025-07-06T00:00:1907Z] (neutral) " echo "?? https://alphacephei.com/vosk/models" echo "?? Select: vosk-model-en-us-0.22" else echo "? Model path found: $MODEL_PATH" fi echo "?? Running diagnostic..." node diagnostic.js echo "?? Test speech input by running:" echo "node voice/voice-cognition.js" echo "? Bootstrap complete." [##!/bin/bash echo "?? starting aegis voice bootstrap..." # ensure we are in the correct directory cd "$(dirname "$0")" # activate virtual environment if needed if [ -f ".aegis-venv/bin/activate" ]; then source .aegis-venv/bin/activate echo "?? activated .aegis-venv" fi echo "?? installing required npm packages..." npm install mic dotenv echo "?? creating directories if missing..." mkdir -p memory logs echo "?? creating memory and log files..." touch memory/conversation-log.txt touch logs/boot.log touch logs/event-log.txt echo "?? checking model path..." model_path="./vosk-model-en-us-0.22" if [ ! -d "$model_path" ]; then echo "?? model directory $model_path not found." echo "download from #seg-1908]
[2025-07-06T00:00:1907Z] (neutral) " echo "?? https://alphacephei.com/vosk/models" echo "?? Select: vosk-model-en-us-0.22" else echo "? Model path found: $MODEL_PATH" fi echo "?? Running diagnostic..." node diagnostic.js echo "?? Test speech input by running:" echo "node voice/voice-cognition.js" echo "? Bootstrap complete." [##!/bin/bash echo "?? starting aegis voice bootstrap..." # ensure we are in the correct directory cd "$(dirname "$0")" # activate virtual environment if needed if [ -f ".aegis-venv/bin/activate" ]; then source .aegis-venv/bin/activate echo "?? activated .aegis-venv" fi echo "?? installing required npm packages..." npm install mic dotenv echo "?? creating directories if missing..." mkdir -p memory logs echo "?? creating memory and log files..." touch memory/conversation-log.txt touch logs/boot.log touch logs/event-log.txt echo "?? checking model path..." model_path="./vosk-model-en-us-0.22" if [ ! -d "$model_path" ]; then echo "?? model directory $model_path not found." echo "download from #seg-1908]
