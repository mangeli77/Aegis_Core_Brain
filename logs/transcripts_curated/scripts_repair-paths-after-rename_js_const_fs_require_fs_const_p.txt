[2025-07-06T00:00:410Z] (neutral) create backup const backupPath = fullPath + '.bak'; fs.copyFileSync(fullPath, backupPath); // Replace and save const updated = content.replaceAll(TARGET_STRING, REPLACEMENT_STRING); fs.writeFileSync(fullPath, updated, 'utf8'); console.log(`?? Fixed: ${path.relative(PROJECT_DIR, fullPath)}`); } } } }); } console.log(`?? Scanning project for "${TARGET_STRING}"...`); walkAndFix(PROJECT_DIR); console.log(`? All occurrences of "${TARGET_STRING}" have been updated to "${REPLACEMENT_STRING}".`); console.log(`?? Backups saved as *.bak if you need to roll back.`); [#// scripts/repair-paths-after-rename.js const fs = require('fs'); const path = require('path'); const project_dir = path.join(__dirname, '..'); const target_string = 'aegis-core'; const replacement_string = 'aegis-soul'; const extensionstomodify = ['.js', '.json', '.md', '.txt', '.env', '.task']; function walkandfix(dir) { fs.readdirsync(dir).foreach(file => { const fullpath = path.join(dir, file); if (fs.statsync(fullpath).isdirectory()) { if (file === 'node_modules' || file === '.git') return; walkandfix(fullpath); } else { const ext = path.extname(file); if (extensionstomodify.includes(ext)) { let content = fs.readfilesync(fullpath, 'utf8'); if (content.includes(target_string)) { // optional #seg-411]
[2025-07-06T00:00:410Z] (neutral) create backup const backupPath = fullPath + '.bak'; fs.copyFileSync(fullPath, backupPath); // Replace and save const updated = content.replaceAll(TARGET_STRING, REPLACEMENT_STRING); fs.writeFileSync(fullPath, updated, 'utf8'); console.log(`?? Fixed: ${path.relative(PROJECT_DIR, fullPath)}`); } } } }); } console.log(`?? Scanning project for "${TARGET_STRING}"...`); walkAndFix(PROJECT_DIR); console.log(`? All occurrences of "${TARGET_STRING}" have been updated to "${REPLACEMENT_STRING}".`); console.log(`?? Backups saved as *.bak if you need to roll back.`); [#// scripts/repair-paths-after-rename.js const fs = require('fs'); const path = require('path'); const project_dir = path.join(__dirname, '..'); const target_string = 'aegis-core'; const replacement_string = 'aegis-soul'; const extensionstomodify = ['.js', '.json', '.md', '.txt', '.env', '.task']; function walkandfix(dir) { fs.readdirsync(dir).foreach(file => { const fullpath = path.join(dir, file); if (fs.statsync(fullpath).isdirectory()) { if (file === 'node_modules' || file === '.git') return; walkandfix(fullpath); } else { const ext = path.extname(file); if (extensionstomodify.includes(ext)) { let content = fs.readfilesync(fullpath, 'utf8'); if (content.includes(target_string)) { // optional #seg-411]
