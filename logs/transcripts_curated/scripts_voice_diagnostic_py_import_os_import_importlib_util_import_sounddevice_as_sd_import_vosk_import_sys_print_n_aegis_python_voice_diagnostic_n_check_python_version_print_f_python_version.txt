[2025-07-06T00:00:2017Z] (neutral) {sys.version.split()[0]} ({sys.executable})") # ? Check if Vosk model path exists MODEL_PATH = "/Users/Aegis/Public/Drop Box/vosk-model-en-us-0.22" if not os.path.isdir(MODEL_PATH): print("? Vosk model path not found:", MODEL_PATH) vosk_model_ready = False else: print("? Vosk model directory found.") vosk_model_ready = True # ? Check Vosk model load try: if vosk_model_ready: model = vosk.Model(MODEL_PATH) print("? Vosk model loaded successfully.") else: model = None except Exception as e: print("? Failed to load Vosk model:", str(e)) model = None # ? Check mic stream try: devices = sd.query_devices() print(f"? {len(devices)} audio devices found.") default = sd.default.device print(f"?? Default input: {devices[default[0]]['name']}") # Attempt mic test stream with sd.InputStream(samplerate=16000, channels=1): print("? Microphone input stream active.") except Exception as e: print("? Mic input stream error:", str(e)) # ? Check Python modules required REQUIRED_MODULES = ["vosk", "soundfile", "speechbrain", "numpy"] for mod in REQUIRED_MODULES: if importlib.util.find_spec(mod): print(f"? Python module found: {mod}") else: print(f"? Python module missing: {mod}") print("\n=== ? DIAGNOSTIC COMPLETE ===\n") [## scripts/voice_diagnostic.py import os import importlib.util import sounddevice as sd import vosk import sys print("\n=== ??? aegis python voice diagnostic ===\n") # ? check python version print(f"?? python version #seg-2018]
